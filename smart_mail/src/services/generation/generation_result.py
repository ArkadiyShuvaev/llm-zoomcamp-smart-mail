from dataclasses import dataclass
from typing import Any


@dataclass
class GenerationResult:
    """
    A data class that represents the result of a text generation operation.

    Attributes:
        input_text_token_count (int): The number of tokens in the input text.
        token_count (int): The number of tokens in the generated output text.
        completion_reason (str | None): The reason for the completion of the text generation.
        output_text (str): The generated by LLM output text.
        text_generation_config (dict[str, Any] | None): The configuration used for text generation.
    """
    input_text_token_count: int
    token_count: int
    completion_reason: str | None
    output_text: str
    text_generation_config: dict[str, Any] | None
